*.log
.DS_Store
.env
.env.backup*
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/backups
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Real past-question papers used by the JAMB extraction tests. Kept out of
# the repository: they are somebody else's compilation, and they are large.
# Drop the documents in and those tests run; without them they skip.
/tests/Fixtures/jamb

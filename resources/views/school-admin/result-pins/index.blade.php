@php
    use App\Enums\ResultCheckingPinStatus;

    $examinationOptions = $examinations->mapWithKeys(fn ($examination) => [
        $examination->id => "{$examination->name} — {$examination->class_name} ({$examination->session})",
    ]);
@endphp

<x-dashboard-layout page-title="Result-Checking PINs" page-subtitle="PINs are generated by EduNest for your school. Assign each one to an examination, then give it to a parent or student.">
    <div class="space-y-6">
        @if (session('status'))
            <div class="rounded-[5px] bg-green-50 p-4 text-sm font-medium text-green-700 dark:bg-green-900/30 dark:text-green-400 lg:rounded-[10px]">
                {{ session('status') }}
            </div>
        @endif

        @if ($errors->any())
            <div class="rounded-[5px] bg-red-50 p-4 text-sm text-red-700 dark:bg-red-900/30 dark:text-red-400 lg:rounded-[10px]">
                <ul class="list-inside list-disc space-y-1">
                    @foreach ($errors->all() as $error)
                        <li>{{ $error }}</li>
                    @endforeach
                </ul>
            </div>
        @endif

        <div class="rounded-[5px] border border-gray-200 bg-white shadow-sm dark:border-gray-700 dark:bg-gray-800 lg:rounded-[10px]">
            <div class="border-b border-gray-100 p-6 dark:border-gray-700">
                <h2 class="text-sm font-bold text-gray-900 dark:text-white">Result-Checking PINs</h2>
                <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">These PINs were generated for your school by EduNest. Assign an unassigned PIN to one of your examinations before handing it to a parent or student — it works for that student only, up to 4 times.</p>
            </div>

            <div class="overflow-x-auto">
                <table class="w-full text-left text-sm">
                    <thead class="bg-gray-50 text-xs uppercase text-gray-500 dark:bg-gray-700/50 dark:text-gray-400">
                        <tr>
                            <th class="px-5 py-3 font-semibold">Code</th>
                            <th class="px-5 py-3 font-semibold">Examination</th>
                            <th class="px-5 py-3 font-semibold">Bound Student</th>
                            <th class="px-5 py-3 font-semibold">Uses</th>
                            <th class="px-5 py-3 font-semibold">Status</th>
                            <th class="px-5 py-3 font-semibold">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100 dark:divide-gray-700">
                        @forelse ($pins as $pin)
                            <tr class="transition-colors duration-200 hover:bg-gray-50 dark:hover:bg-gray-700/50">
                                <td class="px-5 py-3 font-mono font-semibold text-gray-900 dark:text-white">{{ $pin->code }}</td>
                                <td class="px-5 py-3 text-gray-600 dark:text-gray-300">
                                    @if ($pin->examination)
                                        {{ $pin->examination->name }} &middot; {{ $pin->examination->class_name }}
                                    @else
                                        <span class="text-amber-600 dark:text-amber-400">Unassigned</span>
                                    @endif
                                </td>
                                <td class="px-5 py-3 text-gray-600 dark:text-gray-300">{{ $pin->boundStudent?->fullName() ?? '—' }}</td>
                                <td class="px-5 py-3 text-gray-600 dark:text-gray-300">{{ $pin->uses_count }} / {{ $pin->max_uses }}</td>
                                <td class="px-5 py-3">
                                    <span @class([
                                        'rounded-full px-2 py-0.5 text-xs font-semibold',
                                        'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400' => $pin->status === ResultCheckingPinStatus::Active,
                                        'bg-gray-200 text-gray-600 dark:bg-gray-700 dark:text-gray-300' => $pin->status === ResultCheckingPinStatus::Exhausted,
                                        'bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400' => $pin->status === ResultCheckingPinStatus::Revoked,
                                    ])>{{ $pin->status->label() }}</span>
                                </td>
                                <td class="px-5 py-3">
                                    <div class="flex items-center gap-2">
                                        @if (! $pin->examination_id && $pin->status === ResultCheckingPinStatus::Active)
                                            <form method="POST" action="{{ route('result-pins.assign', $pin) }}" class="flex items-center gap-1.5">
                                                @csrf
                                                <select name="examination_id" required class="rounded-[8px] border border-gray-300 bg-white py-1.5 px-2 text-xs text-gray-700 shadow-sm dark:border-gray-600 dark:bg-gray-700 dark:text-gray-200">
                                                    <option value="">Assign to&hellip;</option>
                                                    @foreach ($examinationOptions as $id => $label)
                                                        <option value="{{ $id }}">{{ $label }}</option>
                                                    @endforeach
                                                </select>
                                                <button type="submit" class="rounded-[8px] border border-blue-300 px-3 py-1.5 text-xs font-semibold text-blue-700 transition-all duration-150 hover:-translate-y-0.5 hover:bg-blue-50 dark:border-blue-800 dark:text-blue-400 dark:hover:bg-blue-900/20">Assign</button>
                                            </form>
                                        @endif
                                        @if ($pin->status === ResultCheckingPinStatus::Active)
                                            <form method="POST" action="{{ route('result-pins.revoke', $pin) }}" onsubmit="return confirm('Revoke PIN {{ $pin->code }}? It will stop working immediately.');">
                                                @csrf
                                                <button type="submit" class="rounded-[8px] border border-red-300 px-3 py-1.5 text-xs font-semibold text-red-700 transition-all duration-150 hover:-translate-y-0.5 hover:bg-red-50 dark:border-red-800 dark:text-red-400 dark:hover:bg-red-900/20">Revoke</button>
                                            </form>
                                        @endif
                                    </div>
                                </td>
                            </tr>
                        @empty
                            <tr>
                                <td colspan="6" class="px-5 py-10 text-center text-sm text-gray-500 dark:text-gray-400">No result-checking PINs have been issued to your school yet. Contact EduNest to request some.</td>
                            </tr>
                        @endforelse
                    </tbody>
                </table>
            </div>

            @if ($pins->hasPages())
                <div class="border-t border-gray-100 px-5 py-4 dark:border-gray-700">
                    {{ $pins->links() }}
                </div>
            @endif
        </div>
    </div>
</x-dashboard-layout>
